# Mixle Theme Registry (moved)

The theme registry now lives in its own repository:

**https://github.com/DayMan84/Mixle-Registry**

Publish packs, validate manifests, and update `catalog.json` there. The native Android app loads the catalog from:

`https://raw.githubusercontent.com/DayMan84/Mixle-Registry/main/catalog.json`
